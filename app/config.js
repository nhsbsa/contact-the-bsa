// Use this file to change prototype configuration.

module.exports = {
  // Service name
  serviceName: 'Contact the NHS Business Services Authority (NHSBSA)',

  // Port to run nodemon on locally
  port: 3000
}
